<%= form_for(@scoring_statistic) do |f| %>
  <% if @scoring_statistic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scoring_statistic.errors.count, "error") %> prohibited this scoring_statistic from being saved:</h2>

      <ul>
      <% @scoring_statistic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :scoring_category_id %><br>
    <%= f.collection_select :scoring_category_id, ScoringCategory.order(:name), :id, :name, :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :suggested_points %><br>
    <%= f.text_field :suggested_points %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
